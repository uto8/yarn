<% if resource.errors.any? %>
  <% resource.errors.full_messages.each do |message| %>
    <script>
      toastr.error('<%= message %>', '', { closeButton: true, progressBar: true, positionClass: 'toast-bottom-right' })
    </script>
  <% end %>
<% end %>
